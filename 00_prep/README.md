# Prep

- `sampsheet.sh` — generates `samples.txt` from filenames in `data/`

## Configuration

Rename `prep_scripts/config.sh.example` to `config.sh` and edit parameters before running. `config.sh` is gitignored.